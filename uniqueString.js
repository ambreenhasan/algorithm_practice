function findUniqueCharacters(string){

  for (var i = 0; i < string.length; i++) {
    var letter = string.charAt(i);

    if (letter == string[i + 1]) {
      console.log(value)
    }
  }
}

findUniqueCharacters("robot")